import css from './Footer.module.css'

import React from "react";

const Footer: React.FC = () => {
    return (
        <footer className={css.footer}>made with ❤️</footer>
    )
}

export default Footer